import React from 'react';

const Loader = () => {
  return (
    <div className="Loader">
      <h1>Loading...</h1>
      <h3>...there is a delay (on purpose) so we can see this loader...</h3>
    </div>
  );
};

export default Loader;